neighbor <%= @neighbor %> {
  description "Upstream Provider";
  router-id <%= @router_id %>;
  local-address <%= @ipaddress %>;
  local-as <%= @local_as %>;
  peer-as <%= @peer_as %>;
  hold-time 5;

  static {
    route <%= @anycast_ip %>/32 next-hop <%= @ipaddress %>;
  }
}
