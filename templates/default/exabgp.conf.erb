neighbor <%= @neighbor %> {
  description "Upstream Provider";
  router-id <%= @router_id %>;
  local-address <%= @local_address %>;
  local-as <%= @local_as %>;
  peer-as <%= @peer_as %>;
  hold-time 5;

  static {
    route <%= @anycast_ip %>/24 next-hop <%= @local_address %>;
  }
}
